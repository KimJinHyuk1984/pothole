# 작업지시서 — 「딥러닝 CNN으로 포트홀을 찾아라」 인터랙티브 강의 사이트

> 대상 실행 주체: **Codex**
> 검토·승인 주체: **김진혁 (동양고등학교)**
> 최종 산출물: GitHub Pages 정적 사이트 — `https://kimjinhyuk1984.github.io/pothole`

---

## 0. 이 문서를 읽는 방법

이 지시서는 **STEP 단위로 끊어서 실행**하도록 설계되었다.
Codex는 한 번에 하나의 STEP만 수행하고, **반드시 정지하여 검토를 요청한다.**
승인 없이 다음 STEP으로 넘어가거나 커밋하지 않는다. 자세한 규칙은 §9.

---

## 1. 프로젝트 개요

### 1-1. 무엇을 만드는가

고등학생 대상 딥러닝(CNN) 강의 PPT 184장을 **인터랙티브 웹 교재 21페이지**로 재구성한다.
원본 PPT의 레이아웃·템플릿은 계승하지 않는다. 계승하는 것은 **내용과 교육적 흐름**뿐이다.

### 1-2. 세 가지 핵심 가치

| 가치 | 구체적 요구 |
|---|---|
| **코드는 복사 가능해야 한다** | 원본의 코드 스크린샷 약 100장을 전부 실제 텍스트 코드블록으로 대체. 복사 버튼 · 줄 강조 · 문법 하이라이팅 필수 |
| **개념은 조작할 수 있어야 한다** | 정적 설명 슬라이드를 인터랙티브 위젯으로 전환 (§7) |
| **수업 중에 그대로 띄운다** | 스크롤 문서 ↔ 발표 모드를 한 번의 토글로 전환 (§6) |

### 1-3. 사용 시나리오

1. **교실 프로젝터** — 교사가 발표 모드로 전환해 슬라이드처럼 진행. 무선 프리젠터 사용.
2. **학생 개인 학습** — 스크롤 모드로 자습, 코드 복사해서 실습.
3. **수업 후 복습** — 학생 폰으로 접속.

---

## 2. 기술 스택 및 제약

### 2-1. 스택

- **순수 HTML + CSS + JavaScript (ES Modules).** 빌드 단계 없음.
- 프레임워크(React, Vue, Astro, Next 등) **도입 금지.** 정적 파일을 GitHub Pages에 그대로 올린다.
- 문법 하이라이팅: **Prism.js** (python 컴포넌트 + line-numbers + line-highlight 플러그인)

### 2-2. 절대 규칙 — 외부 CDN 런타임 의존 금지

**모든 외부 자산(폰트, Prism.js, 아이콘)은 리포지터리 안에 로컬로 포함한다.**

이유: 학교 네트워크가 외부 도메인을 차단하는 경우가 실제로 발생한다. 수업 도중 CDN이 막히면 폰트가 깨지고 코드 하이라이팅이 사라진다. `cdn.jsdelivr.net`, `fonts.googleapis.com` 등을 런타임에 호출하는 코드는 어떤 이유로도 작성하지 않는다.

- Pretendard: `vendor/fonts/` 에 woff2 서브셋 포함, `@font-face`로 로컬 참조
- JetBrains Mono: 동일
- Prism.js: `vendor/prism/prism.js`, `prism.css` 로 저장 후 참조

### 2-3. 브라우저 지원

- 최신 Chrome / Edge / Safari / Firefox, 그리고 **iOS Safari, Android Chrome**
- IE 및 레거시 대응 불필요

---

## 3. 파일 구조

```
pothole/
├── index.html
├── p1-1-how-ai-learns.html
├── p1-2-error.html
├── p1-3-neural-net.html
├── p1-4-mlp.html
├── p1-5-mnist-mlp.html
├── p1-6-mlp-limits.html
├── p2-1-why-cnn.html
├── p2-2-conv-pool.html
├── p2-3-mnist-cnn.html
├── p2-4-cnn-limits.html
├── p2-5-augmentation.html
├── p3-1-comnist-gpu.html
├── p3-2-baseline.html
├── p3-3-hyperparams.html
├── p3-4-challenge.html
├── p4-1-dataset.html
├── p4-2-baseline.html
├── p4-3-metrics.html
├── p4-4-results.html
├── p4-5-wrapup.html
├── _kitchen-sink.html          ← 컴포넌트 데모(내부용, 목차에 미노출)
├── css/
│   ├── tokens.css              ← 디자인 토큰 (다크/라이트)
│   ├── base.css                ← 리셋, 타이포, 레이아웃
│   ├── components.css          ← 코드블록, 카드, 위젯, 퀴즈
│   └── present.css             ← 발표 모드 전용
├── js/
│   ├── site.js                 ← 네비, 테마 토글, 진도 저장
│   ├── present.js              ← 발표 모드
│   ├── codeblock.js            ← 복사 버튼, 접기
│   ├── quiz.js
│   └── widgets/                ← 위젯 1개당 파일 1개
│       ├── pixel-grid.js
│       ├── conv-demo.js
│       └── ...
├── data/
│   ├── nav.json                ← 21페이지 목차 (단일 정본)
│   ├── hyperparams.json        ← Part3 비교표 데이터
│   └── shift-results.json      ← 이미지 이동 실험 결과
├── code/                       ← 노트북 원본 (다운로드 링크용)
│   ├── mnist_MLP.ipynb
│   ├── mnist_CNN.ipynb
│   ├── coMNIST_baseline.ipynb
│   ├── comnist.ipynb
│   └── pothole.ipynb
├── assets/                     ← WebP 이미지 (제공됨, 재인코딩 금지)
└── vendor/                     ← 폰트, Prism (로컬)
```

---

## 4. 디자인 시스템

### 4-1. 컨셉

**"야간 도로 위의 개발자"** — 아스팔트 톤의 다크 배경 위에, 노면 표시에서 가져온 옐로우와 안전 오렌지를 액센트로 쓴다. 타이포그래피와 구조는 코드 에디터의 어휘를 빌린다.

프로젝터 밝기 문제를 위해 **라이트 모드**를 함께 제공한다. 라이트 모드에서도 코드블록은 다크를 유지한다.

### 4-2. 토큰 (`css/tokens.css`)

```css
:root, [data-theme="dark"] {
  /* 배경 계열 — 아스팔트 */
  --bg:          #0F1113;
  --bar:         #0A0C0D;
  --surface:     #16191B;
  --surface-2:   #1D2124;
  --line:        #272B2E;

  /* 텍스트 */
  --fg:          #F2EFE9;
  --fg-soft:     #C0BCB4;
  --muted:       #85817A;

  /* 액센트 — 노면 표시 */
  --accent:      #FFC72C;   /* 차선 옐로우 */
  --accent-2:    #FF6B35;   /* 안전 오렌지 */
  --on-accent:   #141618;

  /* 코드 */
  --code-bg:     #08090B;
  --code-head:   #12151800;
  --code-line:   #1E2225;
  --code-fg:     #E4E0D9;
  --code-hl:     rgba(255,199,44,.09);
  --c-key:       #FFC72C;
  --c-str:       #9ED36A;
  --c-num:       #FF9E64;
  --c-fn:        #7FD3E0;
  --c-com:       #6A6F70;

  /* 상태 */
  --ok:          #56D364;
  --warn:        #FF6B35;

  /* 형태 */
  --r-sm: 4px;  --r-md: 8px;  --r-lg: 12px;

  /* 타이포 */
  --font-display: 'JetBrains Mono', ui-monospace, monospace;
  --font-body:    'Pretendard', system-ui, sans-serif;
  --font-mono:    'JetBrains Mono', ui-monospace, monospace;
}

[data-theme="light"] {
  --bg: #FFFFFF;  --bar: #FFFFFF;  --surface: #F7F8FA;  --surface-2: #EEF0F3;
  --line: #E3E6EA;
  --fg: #16191C;  --fg-soft: #40464D;  --muted: #6E757D;
  --accent: #A8620A;  --accent-2: #D9480F;  --on-accent: #FFFFFF;
  /* 코드 토큰은 다크 값을 그대로 상속 — 재정의하지 않는다 */
}
```

### 4-3. 타이포그래피 규칙

- **제목(h1, h2)** — JetBrains Mono 700, `letter-spacing: -.03em`. 한글은 Pretendard로 자동 폴백되도록 폰트 스택 순서 유지.
- **본문** — Pretendard 400/500, 15.5px, `line-height: 1.8`
- **캡션·라벨·수치** — JetBrains Mono 500, 11~13px, `letter-spacing: .12em`, 대문자

### 4-4. 시그니처 요소 (사이트 전체에서 반복)

1. **`$` 프롬프트 마커** — 섹션 eyebrow 앞에 붙는다. `$ Part 2 · 시각 지능의 핵심`
2. **차선 구분선** — 섹션 사이 구분선은 노면 점선.
   `repeating-linear-gradient(90deg, var(--accent) 0 20px, transparent 20px 34px)`, 높이 3px
3. **비유 카드** — 이 강의의 최대 강점은 비유("탐정과 판사", "눈 가린 등산가", "주전 선수 빼고 훈련", "초등학생 1000명 vs 고등학생 10명")다. 전용 컴포넌트로 만들어 전 페이지에서 동일한 형태로 반복 등장시킨다.

### 4-5. 하지 말 것

- 제목 아래 장식용 밑줄, 의미 없는 컬러 스트라이프
- 액센트 컬러를 넓은 면적에 채우기 (옐로우는 포인트로만, 화면의 5% 이내)
- 3종 이상의 폰트 혼용
- 과도한 애니메이션. 전환은 150ms 페이드 수준. `prefers-reduced-motion: reduce`를 반드시 존중.

---

## 5. beat 마크업 규약 ★ 가장 중요

**모든 콘텐츠는 `beat` 단위로 감싼다.** beat = "한 번에 보여줄 한 덩어리".

이 하나의 규약으로 스크롤 모드와 발표 모드가 같은 DOM을 공유한다. 레이아웃을 두 번 만들지 않는다.

```html
<main class="doc">
  <section class="beat" data-beat>
    <h2><span class="num">01</span>필터는 무엇을 하나</h2>
    <p>...</p>
  </section>

  <section class="beat" data-beat data-tall>   <!-- 뷰포트보다 긴 beat -->
    <figure class="code" ...>...</figure>
  </section>
</main>
```

### beat 분할 기준

| 이런 것은 beat 하나 | 이런 것은 beat를 나눔 |
|---|---|
| 개념 문단 1~2개 | 문단 + 다음 개념 문단 |
| 비유 카드 1개 | 비유 카드 + 코드블록 |
| 위젯 1개 | 위젯 + 결과 그래프 |
| 코드블록 1개 + 그 설명 | 코드블록 2개 |
| 결과 이미지 1장 + 해석 | 이미지 + 확인 문제 |

**원본 PPT 슬라이드 1장이 대체로 beat 1개**에 대응한다. 단, 코드를 한 줄씩 나눠 캡처한 구간(§8-2)은 여러 슬라이드를 beat 하나로 합친다.

### `data-tall` 규칙

beat 높이가 뷰포트를 넘으면 (긴 코드블록, 하이퍼파라미터 9열 비교표 등) `data-tall`을 붙인다.

- `data-tall`이 붙은 beat는 발표 모드에서 **스냅을 걸지 않고** 내부 스크롤을 허용한다.
- **폰트를 줄이거나 내용을 잘라서 억지로 한 화면에 맞추지 않는다.** 이건 금지다.
- Codex는 판단이 애매하면 `data-tall`을 붙이고 검토 때 보고한다.

---

## 6. 발표 모드 사양

### 6-1. 동작

- 상단바 버튼 또는 **`P` 키**로 토글. 상태는 `<html data-mode="present">`.
- `localStorage`에 저장하여 페이지 이동 후에도 유지.

### 6-2. 발표 모드에서 바뀌는 것

```css
[data-mode="present"] .doc { scroll-snap-type: y proximity; }
[data-mode="present"] .beat { min-height: 100vh; display: grid; place-content: center;
                              scroll-snap-align: center; }
[data-mode="present"] .beat[data-tall] { min-height: auto; scroll-snap-align: start;
                                          padding-block: 6vh; }
```

- 본문 스케일 **+18%**, 좌우 여백 확대
- 상단바 축소 (로고 + 진행 인디케이터만)
- **교사용 메모(`.note-teacher`)는 숨김** — 스크롤 모드에서만 보인다
- 하단 우측에 조용한 인디케이터 `07 / 14`와 같은 톤의 **`나가기` 버튼**을 항상 표시한다. 버튼을 누르면 발표 모드를 종료한다.
- 발표 모드 진입 직후 화면 하단에 `← → 이동 · B 블랙아웃 · P 또는 Esc 종료` 안내를 표시하고 2초 뒤 150ms 페이드로 숨긴다. `prefers-reduced-motion: reduce`이면 전환 없이 즉시 제거한다.
- 마우스 커서 3초 무동작 시 자동 숨김

### 6-3. 키보드 매핑 (전부 구현할 것)

| 키 | 동작 |
|---|---|
| `→` `↓` `Space` `PageDown` | 다음 beat |
| `←` `↑` `PageUp` | 이전 beat |
| `Home` / `End` | 처음 / 마지막 beat |
| `B` | 블랙아웃 토글 (화면 전체를 `--bg` 단색으로 덮음) |
| `P` | 발표 모드 종료 |
| `Esc` | 블랙아웃 해제 → (이미 해제 상태면) 발표 모드 종료 |

> **`PageUp` / `PageDown`은 반드시 넣는다.** 무선 프리젠터가 보내는 키가 대부분 이것이다. 빠뜨리면 리모컨이 먹통이 된다.

> **`B` 블랙아웃**은 학생 활동으로 넘어갈 때 화면을 죽이는 기능이다. 실제 수업에서 가장 많이 쓰인다.

> 키 판별은 `event.key`가 아니라 물리 키 위치를 나타내는 **`event.code`** (`KeyP`, `KeyB`, `Escape`, `ArrowRight`, `Space` 등)를 사용한다. 한글 입력 상태에서도 발표 키가 동일하게 동작해야 한다.

### 6-4. 만들지 않을 것

- **발표자 노트 별도 창(듀얼 스크린)** — 교실 프로젝터는 대부분 미러링이라 쓸 수 없다. 대신 교사용 메모는 스크롤 모드 인라인으로 제공한다.
- 슬라이드 전용 별도 레이아웃 — beat 구조로 충분하다.

---

## 7. 컴포넌트 사양

### 7-1. 코드블록 ★ 핵심 컴포넌트

```html
<figure class="code"
        data-lang="python"
        data-file="mnist_mlp.py"
        data-highlight="4-5"      <!-- 강조할 줄 -->
        data-collapse="20">       <!-- 20줄 넘으면 접기 -->
  <figcaption>
    <span class="dots"><i></i><i></i><i></i></span>
    <span class="fname">mnist_mlp.py</span>
    <button class="btn-copy">복사</button>
  </figcaption>
  <pre><code class="language-python line-numbers">...</code></pre>
  <div class="code-note"><b>4행</b> 설명 · <b>5행</b> 설명</div>
</figure>
```

요구사항:

1. **복사 버튼** — 클릭 시 코드 전문을 클립보드로. 버튼 라벨이 `복사 → 복사됨`으로 1.4초간 바뀐다. 줄번호는 복사되지 않아야 한다.
2. **줄 강조** — `data-highlight="4-5"`로 지정한 줄에 배경 `--code-hl` + 좌측 3px `--accent` 바. **원본 PPT에서 "여기 보세요" 하며 화살표로 가리키던 부분을 이걸로 재현한다.**
3. **줄번호** — 항상 표시. 설명에서 "4행"으로 지목할 수 있어야 한다.
4. **접기** — `data-collapse` 줄 수를 넘으면 기본 접힘 + "전체 보기" 버튼. 단, 발표 모드에서는 강조된 줄이 반드시 보이도록 접힘 시작점을 조정한다.
5. **가로 스크롤** — 긴 줄은 줄바꿈하지 말고 가로 스크롤. 코드 원문을 훼손하지 않는다.

#### 단계별 줄 강조 (`data-steps`)

하나의 코드블록을 설명 순서대로 짚을 때 `data-steps`에 JSON 배열을 지정한다. 각 단계는 강조 범위 `lines`와 설명 `note`를 가진다.

```html
<figure class="code"
        data-steps='[
          {"lines":"4", "note":"Conv2D가 이미지의 특징을 찾는다."},
          {"lines":"7-8", "note":"Pooling이 중요한 값만 남긴다."}
        ]'>
  ...
  <div class="code-note" aria-live="polite">강조 없음</div>
</figure>
```

- `data-steps` 코드블록은 **코드 전문을 처음부터 표시하는 하나의 beat**다. 단계가 바뀌어도 코드를 숨기거나 한 줄씩 새로 나타내지 않고 강조만 이동한다.
- 초기 상태는 **0단계(강조 없음)**다. 첫 `→`에서 1단계가 시작된다.
- 발표 모드에서 `→`는 현재 코드에 남은 단계가 있으면 다음 단계를 보여주고, 모든 단계를 소진한 뒤 다음 beat로 이동한다.
- `←`는 단계를 역순으로 이동한다. 이전 beat가 단계형 코드이면 그 beat의 마지막 단계로 복귀한다.
- 스크롤 모드와 발표 모드 모두 점 인디케이터와 이전/다음 버튼을 제공한다. 발표 모드에서는 크기와 색의 무게만 낮추되 터치 조작이 가능해야 한다.
- 현재 단계의 `note`를 `.code-note`에 표시하며 150ms 페이드로 전환한다. `prefers-reduced-motion: reduce`에서는 즉시 전환한다.
- `data-collapse`로 접힌 범위에 현재 강조 줄이 있으면 자동으로 전체 코드를 펼친다.
- `data-steps`가 없는 코드블록은 기존 `data-highlight` 동작을 유지한다.
- 전체 단계를 키보드만으로 진행·역행할 수 있어야 한다.

### 7-2. diff 코드블록 (Part 3 전용)

Part 3의 하이퍼파라미터 9종은 베이스라인에서 **몇 줄만 바뀐다.** 탭으로 전환하면 **바뀐 줄만** 초록/빨강으로 표시한다.

```html
<div class="code-diff" data-base="baseline">
  <nav class="difftabs">
    <button data-k="baseline" aria-selected="true">Baseline</button>
    <button data-k="deep">Deep Layer</button>
    ...
  </nav>
  <!-- 각 variant는 data/hyperparams.json에서 로드 -->
</div>
```

학생이 "아, 이 한 줄만 바꾼 거구나"를 즉시 이해하는 것이 목표다.

### 7-3. 비유 카드

```html
<aside class="analogy" data-icon="🔦">
  <h3>비유로 잡기</h3>
  <p>깜깜한 방에서 손전등으로 벽을 훑는다고 생각하자...</p>
</aside>
```

### 7-4. 교사용 메모

```html
<aside class="note-teacher">5 epoch 부근에서 과적합이 시작된다. 여기서 질문 던지기.</aside>
```
스크롤 모드에서만 보인다. 발표 모드에서는 `display:none`.

### 7-5. 확인 문제 (퀴즈)

- 페이지당 1~3문항, 객관식 3~4지선다
- 클릭 즉시 정오 피드백 + 한 줄 해설
- 결과를 `localStorage`에 저장 (`pothole:quiz:<slug>`)

### 7-6. 진도 표시

- 상단바에 진행 바. 현재 페이지 인덱스 / 전체 21
- 방문한 페이지를 `localStorage`(`pothole:visited`)에 기록, 목차에 체크 표시

---

## 8. 콘텐츠 소스와 변환 규칙

### 8-1. 제공되는 자산

| 경로 | 내용 |
|---|---|
| `assets/*.webp` | 원본 PPT 이미지 226장을 WebP로 최적화 (125MB → 7.1MB). 파일명은 `slide-{슬라이드번호:3자리}-{a,b,c...}.webp` |
| `assets-manifest.json` | 이미지별 슬라이드 번호, 원본 크기, 용량 |
| `page-map.json` | 21페이지 ↔ 슬라이드 범위 ↔ 해당 이미지 목록 |
| `code/*.ipynb` | **원본 노트북 5종 — 코드와 해설의 정본** |
| `code/*.py` | 노트북에서 코드+마크다운 해설을 추출한 평문 |
| `code/*.blocks.json` | 섹션 제목 + 코드 단위로 분해한 JSON (웹 코드블록에 그대로 투입 가능) |
| `slide-text.md` | 원본 PPT 184장의 전체 텍스트 (markitdown 추출) |

### 8-2. 이미지 사용 규칙 ★

원본 이미지는 두 종류다. **반드시 구분한다.**

**(A) 코드 스크린샷 — 사용하지 않는다.**
VSCode/Colab 셀을 캡처한 이미지. 노트북의 실제 코드로 대체한다. 대략 100장.
해당 구간: 슬라이드 **37~51, 70~82, 105~126, 148~167**

**(B) 실행 결과 스크린샷 — 그대로 사용한다.**
`model.summary()` 출력표, 학습 로그, 손실/정확도 그래프, 예측 이미지 격자, `nvidia-smi` 출력, 혼동행렬.
"내가 돌리면 이런 화면이 나온다"는 실감이 교육적으로 중요하므로 유지한다.

이미 확인된 분류 (Codex는 이 목록을 신뢰하고 진행):

| 페이지 | **버릴 것 (코드 캡처)** | **쓸 것 (실행 결과)** |
|---|---|---|
| p1-5 | s38a s39a s40a s41a s42a s43b s44b s45b s46a s47a s48a s50a s51b s52b | **s49a** (summary 표) **s51a** (테스트 결과) **s52a** (Acc/Loss 그래프) |
| p2-3 | s70a s70b s71a s72a s72b s73a s73b s74a s74b s75a s75b s76a s76b s78a s79b s82a s82b | **s77a** (summary 표) **s79a s80a s81b** (테스트 결과) **s80b s81a** (Acc/Loss 그래프) |
| p3-2 · p4-2 | 대부분이 코드 캡처 | `summary` 출력 / 학습 로그 / 그래프 / 예측 격자만 |

> p3-2, p4-2 구간은 Codex가 1차로 분류해서 **후보 목록을 제시**하고, 검토 단계에서 확정한다.

**(C) 대체 가능 이미지 — 새로 만들어도 된다.**
표지 히어로, "함께 해봐요" 배지, 아이콘, 장식용 일러스트 등 강의 진행에 직접 영향이 없는 것. SVG/CSS로 직접 제작 가능. 단 **개념 도해(합성곱 진행 과정, 신경망 구조도, 혼동행렬 도식)는 임의로 바꾸지 않는다.**

### 8-3. 텍스트 작성 규칙

- 원본 PPT의 문장은 **개조식 나열이 많다.** 웹에서는 읽히는 문장으로 다듬는다. 단, **개념·수치·비유는 원문을 그대로 보존한다.** 새로운 사실을 지어내지 않는다.
- 노트북의 마크다운 해설이 PPT보다 상세한 경우가 많다. **그럴 땐 노트북 쪽을 본문으로 채택한다.**
- 수치(파라미터 수, 정확도, 학습 시간)는 원본 그대로. 반올림하거나 고치지 않는다.

### 8-4. 외부 링크 (자리만 잡아둔다)

`data/links.json`에 아래 키를 만들고 값은 빈 문자열로 둔다. 값이 비면 버튼을 **비활성 + "준비 중" 표시**로 렌더한다.

```json
{
  "surveyPre": "", "surveyPost": "", "classroom": "",
  "kaggleComnist": "", "kagglePothole": "",
  "worksheet": "", "mnistWebApp": ""
}
```

---

## 9. 작업 순서와 검토 프로토콜 ★

### 9-1. 프로토콜 (모든 STEP에 적용)

```
1. Codex가 STEP N을 구현한다
2. 로컬 확인용 서버 실행 안내: python -m http.server 8000
3. ─── 정지 ───
   아래 형식으로 보고한다:
     · 생성/수정한 파일 목록
     · 구현 내용 요약 (3~6줄)
     · 확인해 주셔야 할 항목 (체크리스트)
     · 판단이 애매해서 임의로 결정한 것 (있으면 반드시 명시)
4. 김진혁 검토 → 수정 요청 시 반영 후 다시 3번
5. 승인 → 그때 커밋 → STEP N+1
```

**승인 전 커밋 금지. 여러 STEP을 묶어서 진행 금지.**
STEP 도중 지시서에 없는 판단이 필요하면 **추측하지 말고 질문한다.**

### 9-2. 커밋 규칙

```
feat(p1-5): MNIST MLP 페이지 + 파라미터 계산기 위젯
fix(code): 복사 버튼이 줄번호까지 복사하던 문제
chore(assets): WebP 이미지 226장 추가
```
STEP 하나 = 커밋 하나.

### 9-3. STEP 목록

| STEP | 내용 | 검토 포인트 |
|---|---|---|
| **0** | 리포 초기화. 디렉터리 구조, `assets/`·`code/` 배치, vendor 폰트·Prism 로컬화, `.nojekyll`, README | 로컬 서버에서 폰트가 뜨는가 (CDN 차단 상태에서도) |
| **1** | `css/tokens.css` + `base.css` + `components.css` + `_kitchen-sink.html`<br>모든 컴포넌트(코드블록, 비유 카드, 퀴즈, 위젯 틀, 표, 이미지 캡션)를 한 페이지에 전시 | **디자인 톤 확정.** 다크/라이트 전환, 코드블록 복사·강조 동작 |
| **2** | `js/present.js` + `css/present.css` + beat 규약<br>_kitchen-sink에서 발표 모드 검증 | 키보드 6종, `B` 블랙아웃, `data-tall` 처리, 커서 숨김 |
| **3** | `index.html` (홈) — 히어로, 21페이지 목차, 강사 소개, 링크 카드 | 히어로 인상, 목차 구조 |
| **4~24** | 페이지 21개를 §10 순서대로 **1페이지 = 1 STEP** | 각 페이지 하단 체크리스트 |
| **25** | 전체 QA — 모바일, 라이트 모드, 링크 점검, 접근성(키보드 포커스, 대비), Lighthouse | 폰에서 확인 |
| **26** | GitHub Pages 배포 설정 + 최종 확인 | 실제 URL 접속 |

### 9-4. 각 페이지 STEP 공통 체크리스트

Codex는 페이지 완료 보고 시 아래를 자가 점검한 결과를 함께 제출한다.

- [ ] 원본 슬라이드 범위의 내용이 누락 없이 반영되었는가
- [ ] 코드 스크린샷을 텍스트 코드블록으로 대체했는가
- [ ] 코드 원문이 노트북과 일치하는가 (생략했다면 명시)
- [ ] 복사 버튼이 줄번호 없이 정확히 복사하는가
- [ ] beat 분할이 자연스러운가 / `data-tall` 표시가 맞는가
- [ ] 발표 모드에서 잘리는 내용이 없는가
- [ ] 모바일 폭 375px에서 깨지지 않는가
- [ ] 라이트 모드에서 대비가 충분한가
- [ ] 이미지에 의미 있는 `alt` 텍스트가 있는가
- [ ] 이전/다음 페이지 링크가 맞는가

---

## 10. 페이지 명세 (21개)

> 표기: **S** = 원본 슬라이드 번호, **NB** = 참조 노트북

### `index.html` — 강의 홈 · S1~7
- 히어로: 강의 제목 + 부제. **새로 제작 가능** (SVG/CSS로 야간 도로 + 포트홀 모티프)
- 4개 Part 목차 카드 → 21페이지 트리
- 강사 소개 (S3 내용 **그대로 유지**, 이메일·사진 포함)
- 링크 카드: 사전 설문 · 구글 클래스룸 (비활성 상태로)
- 오늘의 목표 (S6~7)

### `p1-1-how-ai-learns.html` — 인공지능은 어떻게 학습하나 · S8~17
- 전통 프로그래밍 vs 기계학습 (S9~10) — 비유 카드
- 기계학습의 분류 / 지도학습 (S11~12)
- 컴퓨터가 이미지를 보는 법 (S13~14)
- 레이블 인코딩 vs 원-핫 인코딩 (S15~17) — 원본 비교표 유지
- **위젯 ①** 픽셀 그리드 — 흑백 이미지 위 호버 시 0~255 값 표시, RGB 채널 분리 토글
- **위젯 ②** 인코딩 토글 — 강아지/고양이/새를 Label ↔ One-Hot으로 전환

### `p1-2-error.html` — 오차와 경사하강법 · S18~20
- 오차함수 개념 (S18)
- 오차의 합이 0이 되는 문제 → 절댓값 → 제곱 → MSE (S19)
- 경사하강법: 눈 가린 등산가 (S20) — 비유 카드
- **위젯 ③** MSE 계산기 — S19의 표(x=5,10,15,20 / y=13,31,22,50)를 인터랙티브로. 예측선을 드래그하면 오차·절댓값·제곱·합계가 실시간 갱신. "합이 왜 0이 되는지" 직접 목격시킨다.
- **위젯 ④** 경사하강 시뮬레이터 — 곡면 위 공, **학습률 슬라이더**. 너무 크면 발산하는 것을 체험

### `p1-3-neural-net.html` — 인공신경망과 활성화 함수 · S21~31
- AI 발전 연표 (S22, S29) — 출처 링크 유지
- 구성요소: 노드 · 엣지 · 가중치 · 활성화함수 (S23~25)
- 활성화 함수 4종: Step, Sigmoid, ReLU/LeakyReLU, Softmax (S26~28) — 원본의 장단점 서술 유지
- 왜 오래 정체했나 (S29~31)
- **위젯 ⑤** 활성화 함수 플레이그라운드 — 4종 그래프 + 입력 슬라이더로 출력값 확인. Softmax는 막대 3개를 조절하면 합이 항상 1이 되는 것을 보여줌
- `data-tall` 주의: 활성화 함수 비교표가 길다

### `p1-4-mlp.html` — MLP와 과적합 · S32~35
- 입력층/은닉층/출력층 (S32)
- 층과 유닛이 많으면 좋은가 (S33~34)
- 과소적합 · 과적합 · Early Stopping · Dropout (S33, S35) — 비유 카드 2개("답만 외우기 전에 멈추기", "주전 선수 빼고 훈련")

### `p1-5-mnist-mlp.html` — MNIST를 MLP로 · S36~53 · **NB: mnist_MLP.ipynb**
- MNIST 소개 (S36): 6만/1만, 28×28, 0~255
- 실습 웹앱 링크 (S37) — `links.json`의 `mnistWebApp`
- **코드블록 8개** — 노트북 셀 순서대로. 각 블록에 노트북 마크다운 해설을 붙인다
  1. 모듈 불러오기 2. 랜덤시드 고정 3. 데이터 불러오기 4. 정규화
  5. 모델 설계 (S42~45 대응, **줄 강조로 Flatten/Dense/softmax를 차례로 지목**)
  6. 컴파일 7. 학습 8. 평가 + 그래프
- 파라미터 계산 (S49): 784×128+128 = 100,480
- **위젯 ⑥** 파라미터 폭발 계산기 — **해상도 슬라이더(28×28 → 4K)** 를 움직이면 MLP 파라미터 수가 10만 → 10억으로 튀는 카운터. CNN 값과 나란히 비교. **이 강의에서 가장 강력한 순간이므로 공들여 만든다.**
- 결과 이미지: s49a, s51a, s52a
- 학습 곡선 해석 (S52~53) — 교사용 메모: "5 epoch 부근에서 과적합"

### `p1-6-mlp-limits.html` — MLP의 한계 · S54~57
- 어떤 경우 틀렸나 (S54~55)
- 이미지를 오른쪽 5칸·아래 5칸 이동 → 예측 붕괴 (S56~57)
- **위젯 ⑦** 이동 실험 — **이동 슬라이더(0~5칸)** 로 숫자를 옮기면 MLP 예측 확률이 무너지는 것을 표시. 예측값은 `data/shift-results.json`에 미리 계산해 둔 값 사용 (브라우저에서 추론하지 않는다)

### `p2-1-why-cnn.html` — 왜 CNN인가 · S58~64
- CNN의 등장 배경 (S59)
- MLP vs CNN 비교표 (S60) — 원본 유지
- 특징 추출(눈) / 분류(뇌) (S61~62)
- 왜 마지막에 다시 Flatten? (S63)
- 입력층: 3차원 데이터 (S64) — 피자 박스 비유(교사용 메모에 있음)

### `p2-2-conv-pool.html` — 합성곱층과 풀링층 · S65~69
- 커널/필터, 스트라이드, 패딩 (S65)
- 합성곱 진행 과정 (S66~67) — 원본 도해 유지
- 풀링: Max vs Average (S68) — 원본 표 유지
- 구성요소 요약: 탐정/보고서/판사 (S69) — 비유 카드
- **위젯 ⑧** 합성곱 계산기 — 5×5 입력 위에서 3×3 필터를 한 칸씩 밀며 특성맵을 채운다. 가로/세로 탐지 필터 선택, 스트라이드·패딩 토글. **시안에 프로토타입이 있으니 참고.**
- **위젯 ⑨** 풀링 비교기 — Max와 Average를 나란히. 셀 클릭 시 어느 값이 선택됐는지 하이라이트

### `p2-3-mnist-cnn.html` — MNIST를 CNN으로 · S70~82 · **NB: mnist_CNN.ipynb**
- 코드블록 7개 (모듈 → 차원 늘리기 → 정규화 → CNN 구조 → 컴파일 → 학습 → 평가)
- 파라미터 계산 (S77): 320 / 14×14×32 / 6,272×128+128 / 128×10+10
- 결과 이미지: s77a, s79a, s80a, s80b, s81a, s81b
- MLP와 파라미터 수 비교 → 위젯 ⑥ 재사용

### `p2-4-cnn-limits.html` — CNN도 못 넘은 벽 · S83~86
- 이동시킨 이미지로 다시 테스트 (S84~85)
- 원인 2가지: Flatten의 위치 고정, MaxPooling의 커버 범위 (S86)
- 위젯 ⑦ 재사용 — 이번엔 CNN 예측과 나란히 표시

### `p2-5-augmentation.html` — 데이터 증강 · S87~90 · **NB: mnist_CNN.ipynb (셀 30~38)**
- 데이터 증강 개념과 장점 (S87)
- 코드블록: `RandomFlip / RandomRotation / RandomZoom`
- CNN + 증강, MLP + 증강 결과 비교 (S89~90)
- **위젯 ⑩** 증강 미리보기 — 회전·이동·확대·반전 슬라이더로 실제 이미지가 변형되는 모습 (CSS transform으로 구현)

### `p3-1-comnist-gpu.html` — CoMNIST와 A100 GPU · S91~104
- 학습목표 (S92)
- CoMNIST 데이터셋 (S93~94): 12,825장, 278×278, 26글자
- CPU vs GPU 비유 (S95): 고등학생 10명 vs 초등학생 1000명 — 비유 카드
- 딥러닝은 쩐의 전쟁 (S96)
- **VS Code Remote-SSH 접속 절차** (S99~104) — 단계별 스텝 컴포넌트. **원본 스크린샷 유지** (UI 안내라 필수)
- `data-tall` 필요

### `p3-2-baseline.html` — CoMNIST 베이스라인 · S105~126 · **NB: coMNIST_baseline.ipynb**
- **원본 22장 → 코드블록 12개로 통합.** 이 페이지가 코드 텍스트화의 최대 수혜 지점이다
- 노트북 섹션 순서 그대로: GPU 메모리 확보 → 모듈 → 시드 → 이미지 크기/배치 → 데이터 로드 → 경로 → train/val 데이터셋 → 클래스 백업 → 전처리 함수 → map/캐시 → 데이터 확인 → 증강 → CNN 구조 → 컴파일 → summary → EarlyStopping → 학습 → 평가
- 각 코드블록에 노트북 마크다운 해설을 그대로 배치 (알파 블렌딩 설명 등은 그대로 좋다)
- 이미지: 실행 결과만 선별 (§8-2 참조, 후보 제시 후 확정)

### `p3-3-hyperparams.html` — 하이퍼파라미터 9종 · S127~139
- 9종 각각의 개념 설명 (S128~135): BatchNormalization, Deep Layer, Many Filters, Large Kernels, Stride, Dropout, Batch Size, Optimizer
- **diff 코드블록** (§7-2) — 탭 전환 시 베이스라인 대비 바뀐 줄만 강조
- **위젯 ⑪** 하이퍼파라미터 비교 대시보드
  - 원본 비교표(S136~139)를 정렬 가능한 표로
  - **파라미터 수 × 정확도 산점도** — 점을 클릭하면 해당 모델 설명이 뜬다
  - 학습 시간을 점 크기로
  - 학생이 "가성비 좋은 모델"을 직접 찾게 만드는 것이 목표
- 원본의 결론 4가지 유지: 최고=Deep Layer, 가성비=Stride, 비효율=Many Filters, 특이=BatchNormalization
- `data-tall` 확정

### `p3-4-challenge.html` — CoMNIST 챌린지 · S140~144
- 캐글 제출 안내 (S140~141) — `links.json`의 `kaggleComnist`
- **선생님의 전략 공개** (S142~143): RandomTranslation 5%, 흑백 반전 이유, Rotation 0.05, Block 1~5 구조
- Baseline vs Final 비교표 (0.80345 → 0.96826)
- **NB: comnist.ipynb** 의 최종 모델 코드블록

### `p4-1-dataset.html` — 포트홀 데이터셋 · S145~147
- 데이터 규모: 7,035장 (포트홀 4,025 / 정상 3,010), 8.6GB
- Train 5,626 / Test 1,407
- 데이터 불균형 언급 → p4-2의 `class_weight`로 연결

### `p4-2-baseline.html` — 포트홀 베이스라인 · S148~167 · **NB: pothole.ipynb**
- **원본 20장 → 코드블록 11개로 통합**
- 224×224를 쓰는 이유 (ResNet/VGG/MobileNet 표준)
- 데이터 증강 기하학적 변환 4종
- CNN 구조 · 컴파일(learning_rate 조정) · summary
- **콜백 2개** (EarlyStopping + ReduceLROnPlateau) — CoMNIST와 달라진 점 강조
- `class_weight`로 불균형 처리
- 이미지: 실행 결과만 선별

### `p4-3-metrics.html` — 평가지표 읽는 법 · S168~171
- TP/FP/TN/FN (S168) — 원본 도식 유지
- 정밀도 (S169) — 스팸 필터 예시
- 재현률 (S170) — 암 진단 예시
- AUC / ROC (S171)
- **위젯 ⑫ 혼동행렬 시뮬레이터 ★** — 이 강의 최고의 위젯이 될 자리
  - **임계값 슬라이더(0~1)** 를 움직이면 TP/FP/FN/TN 4칸과 Precision/Recall/F1이 실시간으로 변한다
  - ROC 곡선 위의 현재 위치 점도 함께 이동
  - "스팸 필터 모드" / "암 진단 모드" 버튼 — 어느 지표를 우선해야 하는지 상황별로 체감
  - 데이터는 포트홀 모델의 실제 예측 분포를 근사한 값으로 `data/roc.json`에 저장

### `p4-4-results.html` — 학습 결과 해석 · S172~181
- 5개 그래프 순차 해석: Loss / Accuracy / AUC / Precision / Recall (S172~177)
- 각 그래프 = beat 1개. 그래프 이미지 + 원본의 해석 문장
- 총평 (S178): AUC 0.85, 과소적합 가능성
- 임계값 0.5 적용 결과 (S179~181)
- 교사용 메모: "Train Loss는 내려가는데 Val Loss가 올라가면 과적합 시작"

### `p4-5-wrapup.html` — 마무리 · S182~184
- 캐글 제출 안내 — `kagglePothole`
- 느낀 점 나누기 (S183): Purpose 1, Purpose 2("만능 파라미터는 없다")
- 사후 설문 링크
- 전체 코드 다운로드 (노트북 5종)
- 다음 학습 제안

---

## 11. 위젯 구현 공통 규칙

1. **위젯 1개 = `js/widgets/` 아래 파일 1개.** ES module로 export, 페이지에서 `data-widget="conv-demo"` 속성이 붙은 요소를 찾아 마운트한다.
2. **모든 위젯은 발표 모드에서도 정상 작동해야 한다.** 발표 모드용 별도 구현을 만들지 않는다.
3. **모든 위젯은 키보드로 조작 가능해야 한다.** 슬라이더는 방향키, 버튼은 Enter/Space. 발표 중 리모컨/키보드만으로 시연할 수 있어야 한다.
   단, 발표 모드에서 슬라이더에 포커스가 있을 때는 `←`/`→`가 페이지 이동이 아니라 슬라이더 조작에 쓰이도록 이벤트를 처리한다.
4. **실제 추론은 하지 않는다.** TensorFlow.js를 도입하지 않는다. 예측 결과가 필요한 위젯은 미리 계산한 값을 `data/*.json`에서 읽는다.
5. **모바일에서 터치로 조작 가능해야 한다.** 드래그가 필요한 위젯은 버튼 조작 대안을 함께 제공한다.
6. 위젯 상단에 항상 `직접 해보기` 태그를 붙여 학생이 조작 가능한 영역임을 알린다.

---

## 12. 배포

- GitHub Pages, 브랜치 `main`, 루트 디렉터리
- 리포지터리명 `pothole` → `https://kimjinhyuk1984.github.io/pothole`
- **모든 내부 링크는 상대 경로.** 절대 경로(`/css/...`)를 쓰면 서브패스에서 깨진다
- 루트에 빈 `.nojekyll` 파일 생성 (Jekyll 처리 방지)
- `README.md`에 로컬 실행법, 파일 구조, 콘텐츠 수정 방법 기재

---

## 13. Codex가 하지 말아야 할 것

1. 승인 없이 다음 STEP 진행 또는 커밋
2. 프레임워크·빌드 도구 도입
3. 외부 CDN을 런타임에 호출
4. 제공된 WebP 이미지 재인코딩 또는 리사이즈
5. 노트북 코드를 임의로 "개선"하거나 리팩터링 — 원문 그대로. 생략이 필요하면 명시하고 승인받는다
6. 수치·통계를 반올림하거나 변경
7. 발표 모드에서 내용을 맞추려고 폰트를 줄이거나 텍스트를 자르기
8. `localStorage` 외의 저장소나 외부 API 사용
9. 지시서에 없는 판단을 추측으로 결정 — 질문한다

---

## 14. 첫 실행 지시

> STEP 0을 시작하라. 리포지터리 구조를 만들고, 제공된 `assets/`·`code/`·`data/`를 배치하고, 폰트와 Prism.js를 `vendor/`에 로컬로 포함시켜라. `.nojekyll`과 README를 만들어라.
> 완료 후 **정지**하고 §9-1 형식으로 보고하라.
